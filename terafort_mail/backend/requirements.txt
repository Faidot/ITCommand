Django>=5.0,<6.0
djangorestframework>=3.15
python-decouple>=3.8
# AES-GCM and Argon2id both come from here; no separate argon2 package needed.
cryptography>=44.0
# HTML sanitising for message bodies. Not optional in production: without it
# mailcore.sanitiser falls back to stripping HTML to plain text rather than
# rendering anything unsanitised.
bleach>=6.1
psycopg2-binary>=2.9.9
redis>=5.0
gunicorn>=21.2
whitenoise>=6.6
